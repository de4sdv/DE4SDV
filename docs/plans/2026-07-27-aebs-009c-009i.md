# AEBS INC-009C–009I Completion Implementation Plan

> **For Hermes:** Use subagent-driven-development skill to implement this plan task-by-task.

**Goal:** Deliver repository-defined acceptance boundaries through INC-AEBS-009I with replayable, fail-closed evidence and native SysML v2 verification semantics, without unsupported product, compliance, certification, homologation, or type-approval claims.

**Architecture:** Preserve frozen 009B evidence. Repair 009C in its existing bench. Add one disjoint multi-scenario campaign overlay for 009D–009H, with a closed scenario registry, one immutable retained run per scenario, per-scenario verdicts, independent outcome geometry, and 009B-grade provenance/artifact validation. Model each increment as System 2 evidence contracts plus native verification cases; refine or add System 1 requirement candidates only where the product scope genuinely changes. Bind 009I criteria to an exact controlled source identity and replayable measurements.

**Tech stack:** Python 3.10/3.11, pytest, JSON Schema, YAML, ROS 2 Humble, Autoware Universe pinned runtime, Docker Compose, SysML v2 textual notation, GitHub Actions licensed SysML validation.

---

### Task 1: Freeze scope and correct registry/document drift

**Objective:** Make increment status and ownership text agree with merged PRs and the current registry.

**Files:**
- Modify: `implementation/aebs-autoware-executable-bench/increments.yaml`
- Modify: `implementation/aebs-autoware-executable-bench/README.md`
- Modify: `implementation/aebs-autoware-nominal-vehicle-target-bench/README.md`
- Modify: `scripts/validate_aebs_executable_bench.py`
- Test: `tests/test_validate_aebs_executable_bench.py`

**Steps:**
1. Write failing tests for merged 009B status and D–I ownership text.
2. Run focused tests and verify expected failures.
3. Apply the minimal registry/docs/validator updates.
4. Run focused and repository tests.
5. Commit `docs(aebs): align executable increment status`.

### Task 2: Repair 009C retained-evidence integrity

**Objective:** Bring 009C validation to 009B-grade merge-history, map-runtime, provenance, artifact, and fail-closed controls, then regenerate canonical evidence.

**Files:**
- Modify: `implementation/aebs-autoware-stationary-target-bench/runtime-lock.yaml`
- Modify: `implementation/aebs-autoware-stationary-target-bench/scripts/validate_scenario_evidence.py`
- Modify: `implementation/aebs-autoware-stationary-target-bench/scripts/run_scenario.sh`
- Test: focused 009C evidence-validator tests
- Regenerate: `implementation/aebs-autoware-stationary-target-bench/evidence/009c/`

**Steps:**
1. Add failing tests reproducing squash-merge ancestry rejection and missing authoritative map-runtime checks.
2. Implement commit reachability that remains exact but accepts reviewed branch commits reachable through the recorded merge/PR relation; do not accept arbitrary unrelated commits.
3. Port exact map-runtime and artifact checks from 009B where semantically applicable.
4. Run pure tests and retained-evidence replay.
5. Execute the pinned runtime, quiesce it, validate the new immutable run, and atomically promote canonical evidence.
6. Commit `fix(aebs): restore 009c evidence integrity`.

### Task 3: Create the 009D–H campaign contract core

**Objective:** Establish a closed, immutable scenario registry and pure evaluator interfaces before ROS adapters.

**Files:**
- Create: `implementation/aebs-autoware-scenario-campaign/`
- Create: campaign package contract, registry, evaluator, geometry, schemas, configs, and tests

**Steps:**
1. Write a failing test for one closed 009D nominal-false override control.
2. Implement the minimal typed scenario contract and registry entry.
3. Repeat RED→GREEN vertically for true, stale, missing, malformed, and future-stamped override cases.
4. Repeat for 009E bounded non-activation/false-reaction cases with complete observation windows.
5. Repeat for 009F stale/missing/inconsistent/unavailable System 1 inputs, keeping observer failure inconclusive.
6. Repeat for 009G pedestrian and 009H bicycle crossing-target contracts and independent footprint outcomes.
7. Add mutation tests for unknown keys, non-finite values, contradictory phases, stale/future stamps, incomplete silence windows, duplicate publishers, and stored-verdict tampering.
8. Commit `feat(aebs): add 009d-009h campaign contracts`.

### Task 4: Implement campaign ROS adapters and evidence pipeline

**Objective:** Execute every campaign scenario against the pinned Autoware chain and retain independently replayable evidence.

**Files:**
- Create/modify: campaign fixture, coordinator, observer, collector, launch, runner, evidence builder, validator, runtime lock, Compose file, README, and package metadata
- Test: pure and installed-package contract tests

**Steps:**
1. Write failing adapter tests for configurable override values/timing, target trajectory, input health, and exact publisher topology.
2. Implement minimal ROS adapters while keeping native Autoware risk/diagnostic ownership intact.
3. Add periodic observations and fresh terminal samples for negative/degraded claims.
4. Add source-stamp/receipt-time correlation and exact diagnostic stamp authorization.
5. Add map-frame crossing-target motion and independent ego/target footprint reconstruction.
6. Port 009B atomic promotion, native-log rejection, artifact-role separation, inode/symlink checks, manifest stability, map-runtime checks, and provenance recomputation.
7. Build with one worker and run installed-package tests.
8. Execute each scenario separately; reject failed/inconclusive runs from canonical promotion unless the scenario contract explicitly expects a bounded negative/degraded response and its evaluator returns pass.
9. Commit `feat(aebs): execute 009d-009h scenario campaign`.

### Task 5: Refine System 1 requirement candidates

**Objective:** Add only necessary product-scope obligations for conscious override, false-reaction resistance, degradation, pedestrian, bicycle, and source-backed quantified criteria.

**Files:**
- Modify: `textual-notation-of-model/packages/features/aebs/aebs_needs_requirements.sysml`
- Modify aligned reviewer Markdown/YAML artifacts under `methodologies/sysmod-sysmlv2/pilots/`
- Test: requirements/trace consistency tests

**Steps:**
1. Write failing trace/identifier tests.
2. Refine override and degradation candidates with explicit conditions while retaining candidate status where unresolved.
3. Add separate pedestrian and bicycle needs/requirement candidates; do not reinterpret vehicle-target requirements.
4. Add clause-derived quantified candidates only where applicability and exact source identity are controlled.
5. Record gaps instead of inventing missing tolerances, uncertainty, or authority interpretation.
6. Run requirement/trace tests.
7. Commit `feat(aebs): extend controlled requirement candidates`.

### Task 6: Add native SysML verification models for 009C–I

**Objective:** Represent each increment with System 2 evidence contracts, one objective per case, explicit subjects/methods/data flow/verdicts, and bounded product relevance.

**Files:**
- Create: increment-specific SysML evidence models under `textual-notation-of-model/packages/features/aebs/`
- Create: focused lexical/mutation tests under `tests/`

**Steps:**
1. Write failing tests for native verification structure and forbidden product `verify`/`satisfy` claims.
2. Add 009C case and explicit five-outcome→`VerdictKind` mapping.
3. Add separate configured usages for every 009D–F scenario; never collapse matrix verdicts.
4. Add target-specific 009G/H cases and product-relevance dependencies only to matching new candidates.
5. Add 009I source/criterion/measurement/provenance contracts and configuration-bounded verdict case.
6. Run focused and repository tests; do not substitute lexical tests for licensed validation.
7. Commit `feat(sysml): model aebs 009c-009i verification`.

### Task 7: Implement 009I controlled criterion mapping

**Objective:** Bind exact source/version/digest and selected quantified criteria to replayable campaign measurements with explicit applicability and uncertainty gaps.

**Files:**
- Create: public-safe source metadata and criterion mapping under the campaign bench
- Create: criterion evaluator and tests
- Create: 009I measurement evidence derived from retained 009G/H runs

**Steps:**
1. Write failing tests for source identity, clause mapping, unit conversion, boundary interpolation, tested configuration, and nonclaim enforcement.
2. Implement only the selected criteria supported by retained measurements.
3. Treat missing prescribed conditions, target fidelity, uncertainty, repetitions, or authority interpretation as `inconclusive`/gap—not pass.
4. Retain criterion result, source metadata hash, measurement references, and evaluator identity.
5. Commit `feat(aebs): trace quantified criteria to evidence`.

### Task 8: Integrate, review, and merge

**Objective:** Produce one reviewable PR and merge only on complete evidence and review.

**Steps:**
1. Run all focused tests, repository tests, Ruff/check scripts, smoke test, all canonical evidence validators, and `git diff --check`.
2. Freeze exact staged-diff hash and obtain independent requirements, SysML/V&V, evidence-integrity, and code-quality reviews.
3. Correct findings and repeat exact-hash reviews after every staged change.
4. Push the branch and open a draft PR with per-increment status, executed evidence IDs, claim boundaries, and exact review hashes.
5. Wait for repository and licensed SysML checks; retrieve and fix exact diagnostics on failure.
6. Mark ready only when all checks pass and no blocker/high finding remains.
7. Merge using the repository’s approved PR flow; never bypass review gates or push directly to `main`.
8. Verify merge commit, `origin/main`, canonical evidence replay, and issue/registry closure.
