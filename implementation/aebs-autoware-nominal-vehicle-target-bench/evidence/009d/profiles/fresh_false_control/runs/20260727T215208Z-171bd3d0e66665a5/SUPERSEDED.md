# Superseded validated canary

This run passed its isolated `fresh_false_control` contract and independent replay at repository head `f9812113805138d5a2d4f3dc119c21486f902223`.

It is not canonical for the final 009D matrix because the subsequent `fresh_true_conscious_override` run exposed an observation-order race. The pure evaluator was changed after this run so an exact authorization waits for a late native risk precursor instead of terminating as a diagnostic-authorization error.

The validated evidence bytes are retained as `scenario-evidence.validated-superseded.json` with SHA-256 `942a739cf1a8e44b40f63cfb9e724cdcd2dbce515ee70db94fb75597b6eed601`. No profile success from this run may be aggregated with executions from the repaired head.
