<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.de4sdv.aebsvisualization">

    <application
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.NoActionBar"
        android:allowBackup="false">
        <uses-library android:name="libsdvgatewayclient-java-sdk" android:required="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Render on the center display; cluster/HAR is out of scope. -->
            <meta-data
                android:name="android.car.application.car_launcher_intent"
                android:value="true" />
        </activity>
    </application>
</manifest>
