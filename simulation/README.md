# Simulation

Status: **dormant**.

This directory reserves the integration boundary for FMI, FMU, and SSP assets.
It currently contains no maintained executable simulation baseline.

Activate this area only when an owned increment identifies the decision it
supports, the System 1 subject, tool and version assumptions, reproducible
inputs, expected outputs, and verification evidence. Until then, the directory
is not evidence that DE4SDV provides simulation interoperability.
